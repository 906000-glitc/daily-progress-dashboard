const list=document.querySelector('#taskList');const toast=document.querySelector('#toast');function showToast(message){toast.textContent=message;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2200)}list.addEventListener('change',e=>{if(e.target.matches('input[type=checkbox]')){e.target.closest('.task').classList.toggle('done',e.target.checked);showToast(e.target.checked?'وظیفه تکمیل شد — عالی پیش رفتی!':'وظیفه دوباره به برنامه برگشت')}});document.querySelector('#addTask').addEventListener('click',()=>{const title=prompt('عنوان وظیفه جدید را وارد کن:');if(!title?.trim())return;const item=document.createElement('label');item.className='task';item.innerHTML=`<input type="checkbox"><span class="check"></span><div><strong>${title.trim().replace(/[<>]/g,'')}</strong><small>وظیفه جدید · زمان‌بندی نشده</small></div><time>--:--</time><button class="dots-btn">•••</button>`;list.appendChild(item);showToast('وظیفه جدید به برنامه اضافه شد ✓')});document.querySelectorAll('.task-tabs button').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.task-tabs button').forEach(x=>x.classList.remove('selected'));btn.classList.add('selected')}));document.querySelector('.search').addEventListener('click',()=>{const q=prompt('در برنامه جستجو کن:');if(q)showToast(`جستجو برای «${q}» آماده است`)});document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const target=document.querySelector(a.getAttribute('href'));if(target){e.preventDefault();target.scrollIntoView({behavior:'smooth'})}}));