/* Rava Workspace — clean state, rendering and interaction layer. */
const STORAGE_KEY='rava-workspace-v6';const $=(s,r=document)=>r.querySelector(s);const $$=(s,r=document)=>[...r.querySelectorAll(s)];const safe=v=>String(v??'').replace(/[<>]/g,'').trim();
const defaults={user:{name:'سارا احمدی',email:'sara@rava.local'},theme:'light',note:'',activeProject:'همه پروژه‌ها',reminders:true,tasks:[{title:'مرور اهداف سه‌ماهه',meta:'برنامه‌ریزی · ۳۰ دقیقه',time:'۰۹:۰۰',project:'شخصی',done:true},{title:'طراحی وایرفریم صفحه اصلی',meta:'پروژه شخصی · ۱ ساعت و ۳۰ دقیقه',time:'۱۰:۳۰',project:'محصول روال',done:false},{title:'مطالعه فصل چهارم کتاب',meta:'یادگیری · ۴۵ دقیقه',time:'۱۴:۰۰',project:'یادگیری',done:false},{title:'پیاده‌روی عصرانه',meta:'سلامتی · ۳۰ دقیقه',time:'۱۸:۳۰',project:'سلامتی',done:false},{title:'نوشتن سه نکته روزانه',meta:'ذهن‌آگاهی · ۱۰ دقیقه',time:'۲۱:۰۰',project:'شخصی',done:false}],goals:[{title:'طراحی نسخه اول محصول',value:68,color:'purple'},{title:'یادگیری زبان انگلیسی',value:42,color:'orange'}],projects:[{name:'محصول روال',color:'#756bea',tasks:8,done:5},{name:'شخصی',color:'#42b98b',tasks:12,done:9},{name:'یادگیری',color:'#eda24f',tasks:6,done:3}]};
function load(){try{return {...defaults,...JSON.parse(localStorage.getItem(STORAGE_KEY)||'{}')}}catch{return {...defaults}}}const state=load();let filter='all',modalType=null,seconds=1500,timer=null;
const save=()=>localStorage.setItem(STORAGE_KEY,JSON.stringify(state));function toast(message){const n=$('#toast');n.textContent=message;n.classList.add('show');setTimeout(()=>n.classList.remove('show'),2300)}
function renderTasks(){const items=state.tasks.filter(t=>(filter==='all'||(filter==='done'?t.done:!t.done))&&(state.activeProject==='همه پروژه‌ها'||t.project===state.activeProject));$('#taskList').innerHTML=items.map(t=>{const i=state.tasks.indexOf(t);return `<label class="task ${t.done?'done':''}"><input type="checkbox" data-task="${i}" ${t.done?'checked':''}><div><strong>${safe(t.title)}</strong><small>${safe(t.meta)} · ${safe(t.project)}</small></div><time>${safe(t.time)}</time></label>`}).join('')||'<p class="empty-state">در این فیلتر وظیفه‌ای نیست. یک مورد جدید اضافه کن.</p>';$('#taskCount').textContent=state.tasks.filter(t=>!t.done).length;const done=state.tasks.filter(t=>t.done).length;$('#completionRate').innerHTML=`${Math.round(done/Math.max(state.tasks.length,1)*100)} <em>درصد</em>`}
function renderGoals(){$('#goalList').innerHTML=state.goals.map((g,i)=>`<div class="goal"><div class="goal-title"><span>${safe(g.title)}</span><b>${g.value}%</b></div><div class="goal-progress"><i style="width:${g.value}%;background:${g.color==='orange'?'var(--orange)':'var(--purple)'}"></i></div><div class="goal-meta"><span>${100-g.value}% باقی مانده</span><button data-goal="${i}">+ پیشرفت</button></div></div>`).join('')}
function openModal(type){modalType=type;$('#modalTitle').textContent=type==='task'?'وظیفه جدید':type==='goal'?'هدف جدید':'یادداشت جدید';$('#modalTitleInput').value='';$('#modalDescriptionInput').value='';$('#modal').classList.add('open');$('#modal').setAttribute('aria-hidden','false');$('#modalTitleInput').focus()}function closeModal(){$('#modal').classList.remove('open');$('#modal').setAttribute('aria-hidden','true');modalType=null}
function submitModal(){const title=safe($('#modalTitleInput').value),description=safe($('#modalDescriptionInput').value);if(!title)return toast('لطفاً عنوان را وارد کن');if(modalType==='task')state.tasks.push({title,meta:description||'وظیفه جدید',time:'--:--',project:state.activeProject==='همه پروژه‌ها'?'شخصی':state.activeProject,done:false});if(modalType==='goal')state.goals.push({title,value:0,color:state.goals.length%2?'orange':'purple'});if(modalType==='note')state.note+=`${state.note?'\n':''}${title}`;save();renderTasks();renderGoals();$('#notesInput').value=state.note;closeModal();toast('با موفقیت ذخیره شد ✓')}
function exportData(){const link=document.createElement('a');link.href=URL.createObjectURL(new Blob([JSON.stringify(state,null,2)],{type:'application/json'}));link.download='rava-backup.json';link.click();toast('نسخه پشتیبان آماده شد ✓')}function updateTimer(){const m=String(Math.floor(seconds/60)).padStart(2,'0'),s=String(seconds%60).padStart(2,'0');$('#timerDisplay').textContent=`${m}:${s}`;$('#timerProgress').style.width=`${(1500-seconds)/15}%`}function toggleTimer(){if(timer){clearInterval(timer);timer=null;$('#timerButton').textContent='ادامه جلسه';return}$('#timerButton').textContent='توقف جلسه';timer=setInterval(()=>{seconds--;updateTimer();if(seconds<=0){resetTimer();toast('جلسه تمام شد؛ استراحت کن!')}},1000)}function resetTimer(){clearInterval(timer);timer=null;seconds=1500;$('#timerButton').textContent='شروع جلسه';updateTimer()}
function addWorkspaceModules(){const page=$('.page'),section=document.createElement('section');section.className='workspace-modules';section.innerHTML=`<article class="panel module-card" id="projects"><div class="panel-heading"><div><span class="eyebrow">مدیریت پروژه</span><h2>فضای کارهای تو</h2></div><button class="button soft" data-action="project">+ پروژه</button></div><div class="project-list">${state.projects.map(p=>`<button class="project-item" data-project="${safe(p.name)}"><i style="background:${p.color}"></i><span>${safe(p.name)}</span><b>${p.done}/${p.tasks}</b></button>`).join('')}<button class="project-item selected" data-project="همه پروژه‌ها"><i></i><span>همه پروژه‌ها</span><b>${state.tasks.length}</b></button></div></article><article class="panel module-card" id="calendar"><div class="panel-heading"><div><span class="eyebrow">تقویم هفتگی</span><h2>برنامه نزدیک</h2></div><button class="button soft" data-action="reminder">+ یادآور</button></div><div class="calendar-grid">${['ش','ی','د','س','چ','پ','ج'].map((d,i)=>`<div class="calendar-day ${i===1?'today':''}"><small>${d}</small><b>${26+i}</b><i style="height:${22+i*8}%"></i></div>`).join('')}</div><p class="module-note">۳ رویداد مهم در این هفته · <button data-action="reminder">مدیریت یادآورها</button></p></article><article class="panel module-card" id="reports"><div class="panel-heading"><div><span class="eyebrow">گزارش پیشرفته</span><h2>الگوی عملکرد</h2></div><select><option>این هفته</option><option>ماه گذشته</option></select></div><div class="report-score"><strong>۸۶٪</strong><span>ثبات عملکرد</span><i><b style="width:86%"></b></i></div><div class="report-row"><span>تمرکز عمیق</span><b>۷.۲ ساعت</b></div><div class="report-row"><span>تکمیل به‌موقع</span><b>۸۴٪</b></div><div class="report-row"><span>روزهای فعال</span><b>۱۲ روز</b></div></article></section>`;page.appendChild(section)}
function addAccountButton(){const b=document.createElement('button');b.className='profile-button';b.textContent=state.user.name.split(' ')[0];b.dataset.action='account';$('.top-actions').insertBefore(b,$('.top-actions .avatar'))}function accountDialog(){const name=prompt('نام نمایشی جدید را وارد کن:',state.user.name);if(!name)return;state.user.name=safe(name);$('.profile b').textContent=state.user.name;save();toast('پروفایل ذخیره شد ✓')}
function action(a){if(a==='task'||a==='goal')openModal(a);if(a==='focus'){$('#focus').scrollIntoView({behavior:'smooth'});if(!timer)toggleTimer()}if(a==='export')exportData();if(a==='close')closeModal();if(a==='submit')submitModal();if(a==='project'){const name=safe(prompt('نام پروژه جدید:'));if(name){state.projects.push({name,color:'#756bea',tasks:0,done:0});save();location.reload()}}if(a==='reminder')toast(state.reminders?'یادآور بعدی: جلسه برنامه‌ریزی، فردا ساعت ۹':'یادآورها خاموش هستند');if(a==='account')accountDialog();if(a==='search'){const q=safe(prompt('چه چیزی را جستجو می‌کنی؟'));if(q)toast(`جستجو برای «${q}» آماده است`)}}
function bind(){document.addEventListener('click',e=>{const a=e.target.closest('[data-action]')?.dataset.action;if(a)action(a)});$('#taskList').addEventListener('change',e=>{const i=e.target.dataset.task;if(i===undefined)return;state.tasks[i].done=e.target.checked;save();renderTasks();toast(e.target.checked?'وظیفه تکمیل شد — عالی پیش رفتی!':'وظیفه به برنامه برگشت')});$$('.tabs button').forEach(b=>b.addEventListener('click',()=>{$$('.tabs button').forEach(x=>x.classList.remove('active'));b.classList.add('active');filter=b.dataset.filter;renderTasks()}));$('#goalList').addEventListener('click',e=>{const b=e.target.closest('[data-goal]');if(!b)return;const g=state.goals[Number(b.dataset.goal)];g.value=Math.min(100,g.value+5);save();renderGoals();toast('پیشرفت هدف ثبت شد ✦')});$('.workspace-modules').addEventListener('click',e=>{const item=e.target.closest('[data-project]');if(!item)return;state.activeProject=item.dataset.project;$$('.project-item').forEach(x=>x.classList.toggle('selected',x===item));renderTasks();$('#today').scrollIntoView({behavior:'smooth'})});$('#themeToggle').addEventListener('click',()=>{state.theme=state.theme==='dark'?'light':'dark';document.body.classList.toggle('dark',state.theme==='dark');$('#themeToggle').textContent=state.theme==='dark'?'☀':'☾';save()});$('#timerButton').addEventListener('click',toggleTimer);$('#timerReset').addEventListener('click',resetTimer);$('#notesInput').addEventListener('input',e=>{state.note=e.target.value;save()});$('#modal').addEventListener('click',e=>{if(e.target.id==='modal')closeModal()});document.addEventListener('keydown',e=>{if(e.key==='Escape')closeModal();if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='k'){e.preventDefault();openModal('task')}})}
function init(){document.body.classList.toggle('dark',state.theme==='dark');$('#themeToggle').textContent=state.theme==='dark'?'☀':'☾';$('#notesInput').value=state.note;renderTasks();renderGoals();addWorkspaceModules();addAccountButton();updateTimer();bind()}init();